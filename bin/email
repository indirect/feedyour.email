#!/bin/bash

URL="$1"
POST="$2"

if [[ -z "$URL" ]]; then
   if curl -Is http://localhost:3000 > /dev/null; then
    URL="http://localhost:3000"
  else
    URL="http://feedyouremail.test"
  fi
fi

if [[ -z "$POST" ]]; then
  POST="spec/support/body.json"
fi

set -x
curl -vvv "$URL/email/incoming" \
  -d "@$POST" \
  -H "Content-Type: application/json"
